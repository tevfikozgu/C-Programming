#include <stdio.h>

int main()
{
	int value;
	int i=1;
	int minimum_number=2;
	printf("Number Amount:");
	scanf("%d",&value);
	if (value < minimum_number){
		printf("Enter 2 or more values")"
	else{
		while (i <= minimum_number){
					
		}
	}	
	return 0;
}
